-- init-db.sql
-- This runs only when the database volume is empty (first init).
-- Use this file to create additional roles, schemas or extensions if needed.

-- Example: create a schema for the app (uncomment to use)
-- CREATE SCHEMA IF NOT EXISTS restaurant;

-- You can also create extensions here, for example:
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
